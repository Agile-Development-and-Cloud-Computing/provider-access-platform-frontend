# .env.production

# This file is used during production builds (`npm run build`).
# The API base URL points to the live server for the production environment.

VITE_API_BASE_URL=https://access-platform.azurewebsites.net/api
